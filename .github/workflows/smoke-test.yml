name: Actions Smoke Test

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm Actions can queue
        run: echo "GitHub Actions workflow_dispatch is working."
